#include "stackDraf.h"
#include <stdio.h>

int main() {
    InitStackDraf(0);
    DisplayDraf(0);
    CreateDraf(0);
    DisplayDraf(0);
    CreateDraf(0);
    DisplayDraf(0);
    DisplayDraf(0);
    return 0;
}